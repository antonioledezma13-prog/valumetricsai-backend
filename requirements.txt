# ══ ValuMetrics AI — Dependencias Backend ══
# Instalar: py -3.11 -m pip install -r requirements.txt

# Core
fastapi
uvicorn[standard]
pydantic
pyjwt
python-multipart
python-dotenv

# PDF
reportlab
Pillow

# MongoDB Atlas (async)
motor
pymongo

# PayPal (verificación server-side opcional)
# httpx

# Vision AI — Claude (ya incluido via anthropic SDK en runtime)
# anthropic  ← ya está como dependencia del sistema

# Opcional — solo si usas YOLOv8 local
# ultralytics

# Opcional — solo si conectas Supabase PostGIS
# supabase
